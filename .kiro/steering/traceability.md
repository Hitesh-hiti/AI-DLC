---
inclusion: always
---
# Traceability Standard

Required chain:
REQ -> EPIC -> STORY -> AC -> TC -> SCRIPT -> PR -> CI_RUN -> RESULT -> DEFECT/ENHANCEMENT

No downstream artifact should exist without an upstream reference.
For enhancements, retain the original links and add versioned links for changed behavior.
